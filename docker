FROM node:20

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

RUN npm run build

EXPOSE 3000

CMD ["node", "dist/api/server.js"]

version: '3.8'

services:
  pay1:
    build: .
    ports:
      - "3000:3000"
    environment:
      - STRIPE_SECRET_KEY=sk_test_xxx
      - BASE_URL=http://localhost:3000